\begin{tikzpicture}[
    x=1mm, y=1mm,
    proc/.style={draw, rounded corners, fill=black!4, anchor=center, text width=42mm,
                 minimum height=12mm, inner sep=1.8mm, font=\small, align=center},
    data/.style={draw, rectangle split, rectangle split parts=5, align=left,
                 text width=62mm, inner sep=2.2mm, font=\small},
    datahydrated/.style={data, rectangle split part fill={white,white,white,white,black!8}},
    entry/.style={draw, rectangle split, rectangle split parts=4, align=left,
                  text width=38mm, inner sep=2mm, font=\small},
    store/.style={draw, rounded corners, anchor=center, text width=24mm,
                  minimum height=28mm, inner sep=1.8mm, font=\small, align=center},
    arr/.style={-{Latex[length=2.5mm]}},
    lin/.style={},
]

% --- spine (x = 60) ---
\node[proc] (nutrition) at (60, 0) {Nutrition agent};

\node[data] (plan) at (60, -44) {
    \textbf{Agent plan}
    \nodepart{two} person, rationale, guideline citations
    \nodepart{three} meal: slot in the day
    \nodepart{four} recipe: dish name, preparation instructions
    \nodepart{five} portion: food identifier, display name, mass in grams
};

\node[proc] (hydration) at (60, -98) {Hydration};

\node[datahydrated] (hplan) at (60, -148) {
    \textbf{Hydrated plan}
    \nodepart{two} person, rationale, guideline citations
    \nodepart{three} meal: slot in the day, dish name, preparation instructions
    \nodepart{four} portion: mass in grams
    \nodepart{five} food record: identifier, product name, per-\qty{100}{\gram} nutrients, package size
};

\node[proc] (totaller) at (60, -204) {Totaller};

\node[data, rectangle split parts=4] (totals) at (60, -248) {
    \textbf{Nutrient totals}
    \nodepart{two} day totals per nutrient
    \nodepart{three} per-meal breakdown
    \nodepart{four} data-coverage caveats
};

% --- food knowledge base branch (left of hydration) ---
\node[store] (kb) at (-52, -98) {Food\\knowledge\\base};

\node[entry] (dbentry) at (-8, -98) {
    \textbf{Base entry}
    \nodepart{two} identifier
    \nodepart{three} product name as stored
    \nodepart{four} per-\qty{100}{\gram} nutrient fields, package size
};

% --- main flow (heads only on edges ending at a process) ---
\draw[lin] (nutrition) -- (plan);
\draw[arr] (plan.south) -- (hydration.north);
\draw[lin] (hydration) -- (hplan);
\draw[arr] (hplan.south) -- (totaller.north);
\draw[lin] (totaller) -- (totals);

% --- base lookup branch ---
\draw[lin] (kb.east) -- (dbentry.west);
\draw[arr] (dbentry.east) -- (hydration.west);

\end{tikzpicture}

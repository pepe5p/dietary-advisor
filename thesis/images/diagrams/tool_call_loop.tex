\begin{tikzpicture}[
    x=1mm, y=1mm,
    head/.style={draw, rounded corners, anchor=center, text width=34mm,
                 minimum height=9mm, inner sep=1.2mm, font=\small, align=center},
    life/.style={draw=black!45, dashed},
    arr/.style={-{Latex[length=2mm]}},
    msg/.style={font=\small, align=center},
    tag/.style={font=\small\itshape, text=black!60, align=center},
    halt/.style={circle, fill=black, draw=black, inner sep=0pt, minimum size=1.6mm},
]

% --- lifelines (equal spacing: agent--db == db--API) ---
\node[head] (runner) at (0, 0) {Agent runner\\(local process)};
\node[head] (kb) at (45, 0) {Food knowledge base\\(local DuckDB)};
\node[head] (api) at (90, 0) {Model provider\\API};

\draw[life] (runner.south) -- (0, -94);
\draw[life] (kb.south) -- (45, -94);
\draw[life] (api.south) -- (90, -94);

% --- network round trip out to the provider ---
\draw[arr] (0, -12) -- node[msg, above] {send the whole conversation} (90, -12);
\draw[arr] (90, -22) -- node[msg, above] {tool call returned} (0, -22);
\node[halt] at (90, -22) {};
\node[tag, anchor=west] at (93, -22) {halts for tool call};

% --- local hop to the database ---
\draw[arr] (0, -38) -- node[msg, above] {\texttt{lookup\_foods}(one query)} (45, -38);
\draw[arr] (45, -46) -- node[msg, above] {ranked hits} (0, -46);

% --- second network round trip to resume ---
\draw[arr] (0, -68) -- node[msg, above] {resend the whole conversation,\\ now plus the tool result} (90, -68);
\draw[arr] (90, -78) -- node[msg, above] {generation resumes} (0, -78);

\end{tikzpicture}

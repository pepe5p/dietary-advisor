\begin{tikzpicture}[
    node distance=10mm and 16mm,
    box/.style={draw, rounded corners, align=center, minimum height=9mm, inner sep=2mm, font=\small},
    arr/.style={-{Latex[length=2mm]}},
]

\node[box] (draft) {Draft plan};
\node[box, right=of draft] (critic) {Critic\\ (tool-less; sees\\ totals if enabled)};
\node[box, below=of critic] (refiner) {Refiner\\ (same tools as\\ nutrition agent)};
\node[box, right=of critic] (done) {Approved plan};

\draw[arr] (draft) -- (critic);
\draw[arr] (critic) -- node[above, font=\scriptsize] {no issues} (done);
\draw[arr] (critic) -- node[right, font=\scriptsize] {issues} (refiner);
\draw[arr] (refiner.west) to[out=180,in=270] node[left, font=\scriptsize] {revised plan} (critic.south);

\end{tikzpicture}
